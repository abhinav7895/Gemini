<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap"
      rel="stylesheet"
    />
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="styles.css">
    <title>Gemini</title>
  </head>
  <body>
    <main class="main_container">
        <div class="header">
            <a href="/"><h1 class="heading">Abhinav <span class="heading-ai">AI</span></h1></a>
            <img id="ai-img"  class="ai-img" src="./assets/ai.svg" alt="ai-logo">
        </div>
        <form class="form" action="/prompt" method="post">
            <input autocomplete="off" class="prompt-box" type="text" name="prompt" placeholder="Message Abhinav..." <% if (locals.prompt) { %>
             value="<%= locals.prompt %>"
            <% } %> />
            <button class="submit_btn">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" class="text-white dark:text-black"><path d="M7 11L12 6L17 11M12 18V7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>
            </button>
          </form>
          <% if (locals.result) { %>
           <p id="prompt-answer" class="prompt-answer"><%= locals.result %></p>
          <% } %>
    </main>

    <script type="module" src="script.js"></script>
  </body>
</html>
